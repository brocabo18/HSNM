[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
preDeployCommand = "php /var/www/html/init_db.php"
healthcheckPath = "/health.php"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
startCommand = "/start.sh"
